[project]
name = "compounds-service"
version = "0.1.0"
description = "Compound catalog service"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.0",
    "python-jose[cryptography]>=3.5.0",
    "uvicorn[standard]>=0.44.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=8.4.2",
]
